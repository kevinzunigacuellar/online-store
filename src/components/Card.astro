---
export interface Props {
  name: string;
  price: number;
  image: {
    url: string;
    alt: string;
  };
}

const { name, price, image } = Astro.props;
---

<li>
  <img src={image.url} alt={image.alt} />
  <h2>{name}</h2>
  <p>${price} USD</p>
</li>
